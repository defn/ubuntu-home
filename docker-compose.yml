version: '3.2'

services:
  shell:
    image: imma/devshell
    build: devshell
    network_mode: bridge
    privileged: true
    ports:
      - "2222:22"
    volumes:
      - ${DATA}:/data
      - /var/run/docker.sock:/var/run/docker.sock
      - /tmp/work:/tmp/work
