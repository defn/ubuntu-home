--config ~/config.json -s ~/ --bind 0.0.0.0 --baseUrl localhost --appendPort=false --log --disableRSS --disableSitemap
-p $HOME -c $HOME/etc/nginx.conf

https://github.com/mitchellh/vagrant/issues/4050

--prefix=/jenkins

xcode-select --install

/etc/auto_master # remote home
sudo automount -cv

aws configure set default.s3.signature_version s3v4

brew update-reset; brew update

   13  git clone git@github.com:imma/ubuntu
   14  mv ubuntu/.git .
   27  rm -rf ubuntu
   15  git reset --hard
   21  git submodule update --init --recursive
   29  sudo apt-get install -y jq docker.io tmux gnupg2
   22  make cache
       exec bash
   50  usermod -G docker defn
   46  runmany 'docker pull imma/ubuntu:$1' ubuntu start latest
   96  cp .gitconfig.template .gitconfig
   98  cp .ssh/config.template .ssh/config
   99  ssh-keycan localhost > .ssh/known_hosts
  100 ssh-copy-id -f localhost
