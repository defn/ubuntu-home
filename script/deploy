#!/usr/bin/env bash

function main {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"
  source "$shome/script/profile"

  set -exfu

  if ! "$@" test -d .git; then
    "$@" -A git clone git@github.com:imma/ubuntu
    "$@" mv ubuntu/.git .
    "$@" rm -rf ubuntu
    "$@" git reset --hard
  fi

  "$@" git pull

  "$@" home enable cache || true
  "$@" -t -A script/cibuild || true

  "$@" home disable cache
  "$@" -t -A script/cibuild
}

main "$@"
