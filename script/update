#!/usr/bin/env bash

function main {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"
  source "$shome/script/profile"

  local vip="${1:-172.28.129.250}"

  case "$(uname -s)" in
    Darwin)
      sudo ifconfig lo0 alias "$vip"
      ;;
    Linux)
      sudo ifconfig lo:0 "$vip" up
      ;;
  esac
}

main "$@"
