    1  sudo apt update
    2  sudo apt upgrade
    9  sudo reboot
   23  sudo install -d -o ubuntu -g ubuntu /data
   24  sudo install -d -o ubuntu -g ubuntu /config
   27  git clone git@github.com:imma/ubuntu-home
   28  mv ubuntu-home/.git .
   29  rm -rf ubuntu-home
   30  git reset --hard
   31  cd work/base
   39  git submodule update --init base
   43  script/bootstrap 
   60  cd ../block
   61  script/cibuild
   62  source script/profile 
   63  require
   64  cd
   65  block runmany 'cd $1 && git checkout master'
   67  require
   84  home disable cache
   85  block bootstrap
   86  home enable cache
   92  make cache
  100  git clone git@github.com:imma/admin work/admin
  102  docker login
  103  make download local
  106  sudo ifconfig eth0:0 172.28.128.1 up
  105  make up
  107  make up
