version: '3.2'

services:
  base:
    image: imma/ubuntu:base1
    privileged: true
    ports:
      - "22"
    volumes:
      - type: volume
        source: data
        target: /data
        volume:
          nocopy: true

volumes:
  data:
    external: true
